version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies (none for static site)"
  build:
    commands:
      - echo "Build phase - nothing to build for static site"

artifacts:
  files:
    - '**/*'
